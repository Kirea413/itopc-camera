<Window x:Class="iToPC.Receiver.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="iToPC Receiver" Height="650" Width="720"
        MinHeight="610" MinWidth="680" WindowStartupLocation="CenterScreen">
    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Margin="0,0,0,20">
            <TextBlock Text="iToPC Receiver" FontSize="28" FontWeight="SemiBold"/>
            <TextBlock Text="iPhoneカメラをUSB優先・最小バッファで表示" Foreground="{StaticResource MutedBrush}" Margin="0,4,0,0"/>
        </StackPanel>

        <Border Grid.Row="1" Background="{StaticResource PanelBrush}" CornerRadius="10" Padding="16" Margin="0,0,0,12">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="16"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <StackPanel Grid.Column="0">
                    <TextBlock Text="接続" FontWeight="SemiBold" Margin="0,0,0,8"/>
                    <RadioButton x:Name="UsbModeButton" Content="USB（推奨）" IsChecked="True" Margin="0,0,0,6" Checked="ConnectionModeChanged"/>
                    <RadioButton x:Name="WifiModeButton" Content="Wi-Fi" Checked="ConnectionModeChanged"/>
                </StackPanel>
                <StackPanel Grid.Column="2">
                    <TextBlock Text="iPhoneのIPアドレス（Wi-Fi時のみ）" Foreground="{StaticResource MutedBrush}"/>
                    <TextBox x:Name="PhoneAddressBox" Text="192.168.1.2" Margin="0,5,0,0" IsEnabled="False"/>
                </StackPanel>
            </Grid>
        </Border>

        <Border Grid.Row="2" Background="{StaticResource PanelBrush}" CornerRadius="10" Padding="16" Margin="0,0,0,12">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="16"/>
                    <ColumnDefinition Width="110"/>
                    <ColumnDefinition Width="16"/>
                    <ColumnDefinition Width="110"/>
                </Grid.ColumnDefinitions>
                <StackPanel Grid.Column="0">
                    <TextBlock Text="映像設定" FontWeight="SemiBold" Margin="0,0,0,8"/>
                    <CheckBox x:Name="HardwareDecodeCheck" Content="D3D11VA ハードウェアデコード" IsChecked="True" Margin="0,0,0,7"/>
                    <CheckBox x:Name="VirtualCameraCheck" Content="iToPC Cameraへ出力（1080p / 60fps）" IsChecked="True"/>
                    <Button x:Name="VirtualCameraInstallButton" Content="仮想カメラをインストール"
                            HorizontalAlignment="Left" Margin="0,12,0,0" Padding="10,5"
                            Click="VirtualCameraInstallClicked"/>
                </StackPanel>
                <StackPanel Grid.Column="2">
                    <TextBlock Text="FPS" Foreground="{StaticResource MutedBrush}"/>
                    <ComboBox x:Name="FpsBox" SelectedIndex="0" Margin="0,5,0,0">
                        <ComboBoxItem Content="120"/>
                        <ComboBoxItem Content="60"/>
                        <ComboBoxItem Content="30"/>
                    </ComboBox>
                </StackPanel>
                <StackPanel Grid.Column="4">
                    <TextBlock Text="ポート" Foreground="{StaticResource MutedBrush}"/>
                    <TextBox x:Name="PortBox" Text="5000" Margin="0,5,0,0"/>
                </StackPanel>
            </Grid>
        </Border>

        <Border Grid.Row="3" Background="{StaticResource PanelBrush}" CornerRadius="10" Padding="16" Margin="0,0,0,12">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <StackPanel Grid.Column="0">
                    <TextBlock Text="FFmpegツール（ffplay.exeを指定）" FontWeight="SemiBold"/>
                    <TextBox x:Name="FfplayPathBox" Margin="0,8,10,0"/>
                </StackPanel>
                <Button Grid.Column="1" Content="参照…" VerticalAlignment="Bottom" Click="BrowseFfplayClicked"/>
            </Grid>
        </Border>

        <TextBox Grid.Row="4" x:Name="LogBox" IsReadOnly="True" TextWrapping="Wrap"
                 VerticalScrollBarVisibility="Auto" FontFamily="Cascadia Mono, Consolas"
                 FontSize="12" Background="#090B10" Padding="10"/>

        <Grid Grid.Row="5" Margin="0,16,0,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <TextBlock x:Name="StatusText" Text="停止中" VerticalAlignment="Center" Foreground="{StaticResource MutedBrush}"/>
            <Button Grid.Column="1" x:Name="StartStopButton" Content="受信開始" MinWidth="120"
                    Background="#2878FF" Foreground="White" BorderThickness="0" Click="StartStopClicked"/>
        </Grid>
    </Grid>
</Window>
